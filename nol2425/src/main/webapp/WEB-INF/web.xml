<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <!-- Recurso protegido -->
    <security-constraint>
    <web-resource-collection>
        <web-resource-name>ZonaSegura</web-resource-name>
        <url-pattern>/alumno/*</url-pattern>
        <url-pattern>/profesor/*</url-pattern>
    </web-resource-collection>
    <auth-constraint>
        <role-name>rolalu</role-name>
        <role-name>rolpro</role-name>
    </auth-constraint>
	</security-constraint>

    <!-- Configuración de sesión -->
    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>

    <!-- Definición de roles -->
    <security-role>
        <role-name>rolalu</role-name>
    </security-role>
    <security-role>
        <role-name>rolpro</role-name>
    </security-role>

    <!-- Parámetro de configuración de logs -->
	 <context-param>
    <param-name>log-file-path</param-name>
    <param-value>/home/saul/Escritorio/nol_logs.txt</param-value>
	</context-param>
	
	<filter>
	    <filter-name>Logs</filter-name>
	    <filter-class>dew.main.filtros.Logs</filter-class>
	</filter>
	
	<filter-mapping>
	    <filter-name>Logs</filter-name>
	    <url-pattern>/*</url-pattern> <!-- Todas las rutas -->
	</filter-mapping>

    <!-- Configuración de autenticación FORM -->
    <login-config>
        <auth-method>FORM</auth-method>
        <realm-name>RealmAlumnos</realm-name>
        <form-login-config>
            <form-login-page>/login.jsp</form-login-page>
            <form-error-page>/login.jsp?error=1</form-error-page>
        </form-login-config>
    </login-config>

    <!-- Página de inicio -->
    <welcome-file-list>
        <welcome-file>/LoginServlet</welcome-file>
    </welcome-file-list>

</web-app>
